services:
  domain-exporter:
    image: ghcr.io/kevin197011/kk-domain-exporter:latest
    # build: .  # 取消注释以本地构建
    ports:
      - "8080:8080"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=Asia/Shanghai
