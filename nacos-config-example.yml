# 注意：定时检查已改为每天凌晨3点自动执行

# HTTP服务端口 - 可动态调整（需要重启HTTP服务）
port: 8080

# 日志级别 - 可动态调整日志输出级别
log_level: info

# 请求超时时间（秒） - 可动态调整WHOIS查询超时
timeout: 30

# 域名列表 - 可动态添加/删除域名，配置更新后会立即触发检查
domains:
  - example.com
  - google.com
  - github.com
  - qq.com
  - baidu.com

